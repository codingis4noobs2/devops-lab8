name: OIDC Secrets Demo

on:
  push:
    branches: [main]

permissions:
  id-token: write
  contents: read

jobs:
  fetch-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@e3dd6a429d7300a6a4c196c26e071d42e0343502
        with:
          role-to-assume: arn:aws:iam::047450205639:role/devops-lab8-github-role
          aws-region: us-east-1
          audience: sts.amazonaws.com
          disable-retry: true

      - name: Verify identity (no hardcoded keys)
        run: aws sts get-caller-identity

      - name: Fetch DB password from Secrets Manager
        run: |
          SECRET=$(aws secretsmanager get-secret-value \
            --secret-id devops/lab8/db-password \
            --query SecretString \
            --output text)
          echo "Secret retrieved successfully"
          echo "Password length: ${#SECRET} characters"
